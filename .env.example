# Required: Your Dokploy instance URL
DOKPLOY_URL=https://dokploy.example.com

# Required: API key from Dokploy Settings > API Keys
DOKPLOY_API_KEY=your-api-key-here

# Optional: Transport mode (stdio or httpStream)
# TRANSPORT_TYPE=stdio

# Optional: HTTP settings (only used with httpStream transport)
# PORT=3000
# HOST=0.0.0.0
